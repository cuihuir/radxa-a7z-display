# 命名约定

## 仓库名称

仓库和项目身份统一使用 `radxa-a7z-debian12`。

## 文件命名

- 英文文档使用 `kebab-case` 命名。
- 中文翻译文档在同名基础上追加 `.zh-CN.md`。
- 中英成对文档必须放在同一目录下。

## 目录命名

- 使用功能型目录，例如 `docs/research` 和 `docs/architecture`。
- 不要为了不同语言重复建立同义目录。

## 决策日志条目

- 标题要短且具体。
- 如果条目本身是独立记录，文件名里应包含日期。
- 尽量保持“一条决策一条记录”。
