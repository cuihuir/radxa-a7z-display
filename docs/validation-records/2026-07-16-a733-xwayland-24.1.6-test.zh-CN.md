# A733 XWayland 24.1.6 Glamor 测试

## 目标

验证更新版 XWayland 能否在 KWin 使用闭源 PowerVR renderer、`card0` 继续负责
HDMI KMS 输出的条件下，解决 glamor 软件回退。

## 构建

在 A7Z 上原生构建了与 Debian 13/Trixie 对齐的 XWayland `24.1.6`，构建环境为
Debian 12 加 Bookworm backports：

- XWayland 源码 SHA-256：
  `737e612ca36bbdf415a911644eb7592cf9389846847b47fa46dc705bd754d2d7`；
- 本地前缀安装 xorgproto `2024.1` 头文件；
- libdrm `2.4.123`、Mesa/GBM `25.0.7`、wayland-protocols `1.43`；
- 单线程构建，临时增加 2 GiB swap；
- 最终 ARM64 二进制 SHA-256：
  `a58c2908da43d45bdf28f63b692c260452b9f455ce9aa36cd52a33190f389135`。

二进制只安装在 `/home/radxa/xwayland-build/prefix`，没有替换 Debian 的
`xwayland 22.1.9-1` 软件包。

## 结果

| XWayland | 图形环境 | 结果 |
|---|---|---|
| `22.1.9` | 发行版 Mesa | `egl: failed to create dri2 screen`，回退软件绘制 |
| `22.1.9` | PowerVR DRI/EGL | 加载 `pvr_dri.so` 后仍回退软件绘制 |
| `24.1.6` | 发行版 Mesa | `EGL setup failed, disabling glamor` |
| `24.1.6` | PowerVR DRI/EGL | `Main linux-dmabuf device has no render node` |

所有组合中，XWayland 都同时打开 `/dev/dri/card0` 和
`/dev/dri/renderD128`，因此问题不是缺少文件描述符或访问权限。

## 多 GPU 设备顺序测试

还测试了 `KWIN_DRM_DEVICES=/dev/dri/card1:/dev/dri/card0`，尝试把 PowerVR
节点作为 KWin 主 DRM 设备，但 `card1` 不是完整的 KMS 设备，结果为：

- `card1` 不支持 KWin 所需的 atomic modesetting；
- KWin 无法创建 linux-dmabuf feedback table；
- 跨 GPU AR24 framebuffer 创建返回 `EINVAL`；
- XWayland 退出，Plasma 启动超时。

该配置已经通过 SSH 立即恢复。

## 结论

仅升级 XWayland 无法启用 glamor。当前拓扑是：KMS 显示设备 `card0` 没有
render node，而闭源渲染设备 `card1`/`renderD128` 又不具备 KWin 对主 DRM GPU
要求的 KMS 能力。下一步应在保持 `card0` 为主 KMS 设备的前提下，研究或修改
KWin 针对这种分离拓扑的 linux-dmabuf feedback 选择逻辑。

板卡已恢复为 XWayland `22.1.9`、`gpu6` 清洁 wrapper、`default l0` 和正常
SDDM 登录界面；临时自动登录及 swap 文件均已删除。

## Render Node Feedback 后续验证

2026-07-17 临时将 KWin 的 linux-dmabuf feedback 主设备改为 EGL render node
`/dev/dri/renderD128`，同时继续使用 `/dev/dri/card0` 负责 KMS。XWayland
`24.1.6` 随后越过此前的 `Main linux-dmabuf device has no render node` 错误，
并成功加载 `pvr_dri.so`。这验证了 KWin render node 选择补丁的方向。

还在板端使用 Debian 实际 KWin 构建依赖，将补丁后的 KWin 5.27.5
`egl_dmabuf.cpp` 原生编译为 ARM64 object。编译成功，只有补丁范围之外、上游原有
的 YUV aggregate 初始化 warning。这确认 EGL device-query API 用法与目标板源码
和工具链兼容。

桌面 OpenGL 探测失败后，XWayland 会自动继续尝试 GLES2。depth 30 format
warning 只会将该格式标记为 texture-only，不会禁用整个 glamor 后端。离屏执行
X11 pixmap-to-pixmap CopyArea 时，PowerVR 内核计数从空闲升到 GPU 总利用率
100%、3D 利用率约 74%。由于该 workload 不更新任何窗口，这直接证明 XWayland
的 GLES glamor 在使用 GPU，而不是只有 KWin 在合成结果。

最初的客户端侧 X11 probe 报告：

- 桌面 OpenGL context 建立时报 `Failed to make EGL context current with GL`；
- 厂商包提供 EGL 与 OpenGL ES，但不提供桌面 OpenGL `libGL.so`；
- GLX 无法创建 PVR DRI3 screen，继续使用 llvmpipe；
- X11 EGL/OpenGL ES 客户端可以选择 PVR DRI3 driver 和配置，但
  `eglMakeCurrent()` 返回 `EGL_BAD_MATCH`；
- `glxinfo -B` 显示 Mesa llvmpipe，`Accelerated: no`。

Mesa DRI2 EGL 实现会在厂商驱动级
`driBindContext(context, draw, read)` 拒绝 drawable 时返回
`EGL_BAD_MATCH`；后续 `DRI2: failed to rebind the previous context` 表示旧
driver context 的恢复同样失败。在此之前，设备、DRI3、EGL config 和 X11
visual 选择都已经成功，因此剩余失败边界位于闭源 PVR DRI 的
context/drawable 绑定。参见
[Mesa `egl_dri2.c`](https://sources.debian.org/src/mesa/26.1.5-1/src/egl/drivers/dri2/egl_dri2.c/#L1444)。

这最初将失败边界定位在闭源 DRI 的 context/drawable 绑定。后续逐 config probe
修正了这个结论。

## X11 EGL 原生 Visual 修正

`tools/a733_x11_egl_probe.c` 会枚举每个 EGL config，并使用该 config 的
`EGL_NATIVE_VISUAL_ID` 创建 X11 窗口，而不是沿用早期 probe 的默认 X11
visual。全部 36 个支持 GLES2 的 config 都成功创建 pbuffer 和 window surface、
绑定 context、清屏并 swap，报告如下：

```text
GLES vendor=Imagination Technologies
renderer=PowerVR B-Series BXM-4-64
version=OpenGL ES 3.2 build 24.2@6603887
summary: tested=36 window_successes=36 render_successes=36
```

因此此前的 `EGL_BAD_MATCH` 来自原生 visual 不匹配，而不是 PowerVR 普遍拒绝
X11 EGL drawable。客户端侧 X11 EGL/GLES 已可用；厂商栈不提供桌面 OpenGL
`libGL.so`，所以桌面 GLX 仍是独立限制。

## 打包集成验证

2026-07-17 将补丁构建为 KWin `4:5.27.5-3+a7331` 包，并与
`a733-pvr-gpu 24.2.6603887+gpu7`、`a733-pvr-xwayland 24.1.6+a7331`
一同安装。真实 SDDM Plasma Wayland 会话成功启动。运行中的 XWayland 使用
`/opt` 下的 payload 并带有 `-glamor es`；KWin 与 XWayland 均持有
`/dev/dri/renderD128` 文件描述符，KMS 仍固定在 `/dev/dri/card0`。

打包路径再次运行离屏 `x11perf -copypixpix500`，达到每秒 843 次操作。
PowerVR 利用率达到 99-100%，其中 geometry 约 25%、3D 约 74%。驱动状态保持
OK，没有 server、HWR、WGP 或 TRP 错误。验证完成后已删除临时自动登录、构建
源、swap 和回滚资源；`gpu7` 集成继续保留在板卡上。

这批首次完整 KWin 包属于集成测试产物，因为部分原生编译为缩短时间使用了较低
优化。正式发布前必须通过 `tools/build_a733_kwin.sh` 使用 Debian 默认优化重新
构建，并再次完成本节实机验证。
